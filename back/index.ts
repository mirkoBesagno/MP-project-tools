import { Server } from "http";
import { Main } from "./main";
var parallel = require('run-parallel');
import { ServerExress } from "./server";

try {
    Main();
    //ServerExress();

} catch (error) {

}